name: CI Docker Build & Push

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.sirnabil }}
          password: ${{ secrets.dckr_pat_Vbuf6F6EyLnvb1TG7OB6QLM1v28 }}

      - name: Build Docker image
        run: docker build -t sirnabil/my-devops-app:latest .

      - name: Push Docker image
        run: docker push sirnabil/my-devops-app:latest
